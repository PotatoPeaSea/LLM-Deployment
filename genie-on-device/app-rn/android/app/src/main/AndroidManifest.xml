<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- INTERNET is the web_search tool; the model weights are pushed by adb. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Tool calling. Both are requested at runtime when a tool-capable chat
         opens, and a refusal is reported back to the model as a tool result
         rather than crashing the turn. See Tools.kt. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <!-- Image attachment needs no permission at all: ImagePickerModule goes
         through the system document picker, which grants access to exactly the
         one file the user chose. READ_MEDIA_IMAGES would let this app read
         every photo on the device to implement the same feature. -->

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:largeHeap="true"
      android:theme="@style/AppTheme">

      <!-- libQnnHtpV73Stub.so dlopens the FastRPC client to reach the DSP. It is
           a public vendor library, but an app's classloader namespace only
           exposes such libraries when they are declared here. Without this,
           QnnDevice_create fails with "Failed to load skel, error: 4000". -->
      <uses-native-library android:name="libcdsprpc.so" android:required="true" />
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
