<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- The whole point of the app: no inference leaves the device. -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@android:drawable/ic_menu_send"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/Theme.GenieChat">

        <!-- libQnnHtpV73Stub.so dlopens the FastRPC client to reach the DSP.
             It is a public vendor library (/vendor/etc/public.libraries.txt),
             but an app's classloader namespace only exposes such libraries if
             they are declared here. Without this, QnnDevice_create fails with
             "Failed to load skel, error: 4000" and the model never loads. -->
        <uses-native-library android:name="libcdsprpc.so" android:required="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
